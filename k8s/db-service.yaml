apiVersion: v1
kind: Service
metadata:
  name: votesystem-db
  namespace: default
  labels:
    app: votesystem-db
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: mysql-primary
